# v0.9.23 — Stable module shelves and a readable finale

- Pickup shelves in Developer Desk remain stable, so modules cannot hang in the air after their support collapses. Other unstable footholds remain in the route.
- The final objective rests on the desk as a code module with a small flag.
- The exit stays visibly locked while its guard is alive and distinguishes a missing objective from an undefeated guard.
- Regression checks cover module placement, collectible clearance, single-hit shields, healing at full health, and clearing boosts after falls.

Validation: 128 Bug Hunt tests, TypeScript/production build, and browser rendering checks for module shelves and all three exit states. These checks are not a complete native desktop playthrough; the Wails CLI is unavailable.
