# {{.ServerName}} MCP Server

Auto-generated by adOmnia from a collection of {{len .Tools}} API endpoints.

## Setup

```bash
npm install
cp .env.example .env
```

## Run

```bash
npx ts-node index.ts
```

## Connect

```json
{
  "mcpServers": {
    "{{.ServerName}}": {
      "command": "npx",
      "args": ["ts-node", "/path/to/{{.ServerName}}/index.ts"]
    }
  }
}
```

## Tools
{{- range .Tools}}

### `{{.Name}}`

{{.Description}}

**Method:** `{{.Method}}` `{{.OriginalURL}}`
{{- end}}
