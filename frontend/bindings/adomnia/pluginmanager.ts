// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import { Call as $Call, CancellablePromise as $CancellablePromise, Create as $Create } from "@wailsio/runtime";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as plugins$0 from "./internal/plugins/models.js";

/**
 * ApplyEventJSON runs all hooks sequentially and returns the final payload.
 * A hook error aborts the operation so callers never silently send a request
 * that an enabled plugin failed to prepare.
 */
export function ApplyEventJSON(eventType: string, payloadJSON: string): $CancellablePromise<string> {
    return $Call.ByID(3138452108, eventType, payloadJSON);
}

/**
 * DisablePlugin disables a plugin and unregisters its hooks.
 */
export function DisablePlugin(id: string): $CancellablePromise<void> {
    return $Call.ByID(2410519057, id);
}

/**
 * EmitEvent fires an event through the hook system and collects results from all registered handlers.
 */
export function EmitEvent(event: plugins$0.PluginEvent): $CancellablePromise<plugins$0.HookResult[]> {
    return $Call.ByID(1848808691, event).then(($result: any) => {
        return $$createType1($result);
    });
}

/**
 * EnablePlugin enables a plugin and registers its hooks.
 */
export function EnablePlugin(id: string): $CancellablePromise<void> {
    return $Call.ByID(2278470786, id);
}

/**
 * ExecuteAction invokes an action declared by an enabled plugin.
 */
export function ExecuteAction(pluginID: string, actionID: string, args: { [_ in string]?: any }): $CancellablePromise<plugins$0.ExecResult> {
    return $Call.ByID(328855, pluginID, actionID, args).then(($result: any) => {
        return $$createType2($result);
    });
}

/**
 * FireEvent queues a plugin event for async delivery.
 * Returns false if the bus is full and the event was dropped.
 */
export function FireEvent(event: plugins$0.PluginEvent): $CancellablePromise<boolean> {
    return $Call.ByID(1596910622, event);
}

/**
 * GetAvailableEvents returns the list of all hookable event names.
 */
export function GetAvailableEvents(): $CancellablePromise<string[]> {
    return $Call.ByID(3176042976).then(($result: any) => {
        return $$createType3($result);
    });
}

/**
 * GetPlugin returns a single plugin by ID.
 */
export function GetPlugin(id: string): $CancellablePromise<plugins$0.PluginInstance | null> {
    return $Call.ByID(3817304437, id).then(($result: any) => {
        return $$createType5($result);
    });
}

/**
 * GetPluginSettings returns the settings map for a plugin.
 */
export function GetPluginSettings(id: string): $CancellablePromise<{ [_ in string]?: string }> {
    return $Call.ByID(110070424, id).then(($result: any) => {
        return $$createType6($result);
    });
}

/**
 * GetPlugins returns all installed plugins.
 */
export function GetPlugins(): $CancellablePromise<plugins$0.PluginInstance[]> {
    return $Call.ByID(876014706).then(($result: any) => {
        return $$createType7($result);
    });
}

/**
 * GetRegisteredHooks returns a map of event names to the list of plugin IDs with registered handlers.
 */
export function GetRegisteredHooks(): $CancellablePromise<{ [_ in string]?: string[] }> {
    return $Call.ByID(3398471290).then(($result: any) => {
        return $$createType8($result);
    });
}

/**
 * Init scans the plugin directory, loads manifests, and registers hooks for enabled plugins.
 */
export function Init(): $CancellablePromise<void> {
    return $Call.ByID(902492900);
}

/**
 * InstallPlugin installs a plugin from manifest JSON, creating its directory and saving manifest.json.
 */
export function InstallPlugin(manifestJSON: string): $CancellablePromise<plugins$0.PluginInstance | null> {
    return $Call.ByID(1241939322, manifestJSON).then(($result: any) => {
        return $$createType5($result);
    });
}

/**
 * InstallPluginDirectory reads a directory selected through the native Wails
 * picker and installs it as a complete package. Links and special files are
 * rejected so the selected tree cannot escape its root or block the reader.
 */
export function InstallPluginDirectory(sourceDir: string): $CancellablePromise<plugins$0.PluginInstance | null> {
    return $Call.ByID(2566198873, sourceDir).then(($result: any) => {
        return $$createType5($result);
    });
}

/**
 * InstallPluginPackage installs a complete local plugin folder. File contents
 * are provided as base64 so JavaScript files and assets survive the desktop
 * bridge without browser filesystem assumptions.
 */
export function InstallPluginPackage(manifestJSON: string, encodedFiles: { [_ in string]?: string }): $CancellablePromise<plugins$0.PluginInstance | null> {
    return $Call.ByID(1738750450, manifestJSON, encodedFiles).then(($result: any) => {
        return $$createType5($result);
    });
}

/**
 * LoadPluginState restores plugin state from bbolt.
 */
export function LoadPluginState(): $CancellablePromise<void> {
    return $Call.ByID(2700767946);
}

/**
 * SavePluginState persists enabled/disabled state and settings to bbolt.
 */
export function SavePluginState(): $CancellablePromise<void> {
    return $Call.ByID(3700742405);
}

/**
 * SetPluginSetting updates a single setting value for a plugin.
 */
export function SetPluginSetting(id: string, key: string, value: string): $CancellablePromise<void> {
    return $Call.ByID(887268631, id, key, value);
}

/**
 * Shutdown signals the event dispatch loop to exit.
 */
export function Shutdown(): $CancellablePromise<void> {
    return $Call.ByID(1954807296);
}

/**
 * UninstallPlugin removes a plugin directory and unregisters its hooks.
 */
export function UninstallPlugin(id: string): $CancellablePromise<void> {
    return $Call.ByID(4068609467, id);
}

// Private type creation functions
const $$createType0 = plugins$0.HookResult.createFrom;
const $$createType1 = $Create.Array($$createType0);
const $$createType2 = plugins$0.ExecResult.createFrom;
const $$createType3 = $Create.Array($Create.Any);
const $$createType4 = plugins$0.PluginInstance.createFrom;
const $$createType5 = $Create.Nullable($$createType4);
const $$createType6 = $Create.Map($Create.Any, $Create.Any);
const $$createType7 = $Create.Array($$createType4);
const $$createType8 = $Create.Map($Create.Any, $$createType3);
